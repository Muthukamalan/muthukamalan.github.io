{
  "name": "jekyll-container",
  "runArgs": [
    "--name","blog.io"
  ],
  "build": {
    "context": "..",
    "dockerfile": "../Dockerfile"
  },
  "mounts": [
    "source=jekyll-bundle,target=/usr/local/bundle,type=volume"
  ]
}